# Dependencies & build
/node_modules
/.next
/out
*.tsbuildinfo

# Next.js — regenerated by `next dev` / `next build` (do not commit churn or .next/types refs)
/next-env.d.ts

# Env (never commit secrets). `.env.example` is committed as a template.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env*.local

# Logs & OS
*.log
npm-debug.log*
.DS_Store
Thumbs.db
__MACOSX/
._*

# IDE
.idea
.vscode

# Accidental nested copy of this repo
register-landing/
